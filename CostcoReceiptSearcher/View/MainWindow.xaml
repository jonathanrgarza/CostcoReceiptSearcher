<Window
    x:Class="CostcoReceiptSearcher.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="clr-namespace:CostcoReceiptSearcher.Model"
    xmlns:viewModel="clr-namespace:CostcoReceiptSearcher.ViewModel"
    mc:Ignorable="d"
    Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Target="SearchTextField">Search: </Label>
            <TextBox Grid.Column="1" Name="SearchTextField" HorizontalAlignment="Stretch" Text="{Binding SearchText}" />
        </Grid>
        <Button Grid.Row="1" Content="Search" Command="{Binding SearchCommand}" Margin="5" />
        <ListView Grid.Row="2" ItemsSource="{Binding MatchingPdfFiles}" Margin="5">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type model:PdfFile}">
                    <ListBoxItem MouseDoubleClick="ListBoxItem_MouseDoubleClick">
                        <TextBlock Text="{Binding FileName}" />
                    </ListBoxItem>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>